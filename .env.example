# Optional: dedicated GET URL for the services menu (Google Apps Script web app).
# Defaults to GOOGLE_SHEETS_WEB_APP_URL when unset.
# VITE_GOOGLE_SHEETS_SERVICES_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
